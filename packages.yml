---

- name: Install required runtime dependencies
  hosts: spotlight

  tasks:
    - name: Install distro packages
      package:
        name: '{{ item }}'
        state: present
      become: true
      loop:
        - '{{ runtime_packages }}'
