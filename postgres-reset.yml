---

- name: Resets pgsql db
  hosts: spotlight

  roles:
    - name: geerlingguy.postgresql
      become: true
      postgresql_databases:
        - name: spotlight_db
          state: absent
      # "omit" doesn't work for postgresql_users
      postgresql_users: []

    - name: geerlingguy.postgresql
      become: true
